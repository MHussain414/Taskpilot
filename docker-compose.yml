services:
  taskpilot:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_HOST: 0.0.0.0
      FLASK_SECRET_KEY: change-me-in-production
    volumes:
      - ./instance:/app/instance
